import styled from '@emotion/styled';

export const SectionBox = styled.div`
  text-align: center;

  margin: 10px;
  padding: 8px;
  width: 320px;

  background-color: beige;
  border: 1px solid #cec074;

  box-shadow: 0.1em 0.1em 0.3em rgba(0, 0, 0, 0.2);
  border-radius: 4px;
`;
